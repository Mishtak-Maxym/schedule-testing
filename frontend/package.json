{
  "name": "schedule-testing-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "JavaScript/Jest unit tests for schedule group view module",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "stryker": "stryker run"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^8.7.1",
    "@stryker-mutator/jest-runner": "^8.7.1",
    "jest": "^29.7.0"
  }
}
